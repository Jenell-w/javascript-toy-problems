//find unique number in an array.  so far prints out the num before, the unique num, and num after;
function findUniq(arr) {
  for (let i =0; i < arr.length;  i++) {
    if (arr[i] !== (arr[i-1] || arr[i]!== arr[i+1])) {
      console.log(arr[i])
    }
  }
}
