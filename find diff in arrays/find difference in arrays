//Return the symmetric difference of 2 arrays

function diffArray(arr1, arr2) {
  let unique1 = arr1.filter(x => arr2.indexOf(x) === -1 );
  let unique2 = arr2.filter(x => arr1.indexOf(x) === -1 );
  let newArr = unique1.concat(unique2)
  return newArr
}
